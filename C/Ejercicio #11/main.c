#include <stdio.h>
#include <stdlib.h>
#include <windows.h>
/*
int main(int argc, char *argv[]) {
	FILE *arch;
	arch = fopen("carpetaDePrueba\\archivo.txt", "w");
	if(arch == NULL) {
		arch = fopen("prueba.bat", "w");
		arch = fopen("prueba.bat", "a");
		fprintf(arch, "@echo off\nMD carpetaDePrueba");
		fclose(arch);
		system("prueba.bat");
	} else {
		arch = fopen("carpetaDePrueba\\archivo.txt", "a");
		fprintf(arch, "15");
		fclose(arch);
	}
	
	system("PAUSE");
	return 0;
}*/
